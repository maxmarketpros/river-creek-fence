---
import "@/styles/globals.css";
import "@fontsource/plus-jakarta-sans/400.css";
import "@fontsource/plus-jakarta-sans/500.css";
import "@fontsource/plus-jakarta-sans/600.css";
import "@fontsource/plus-jakarta-sans/700.css";
// Build-hashed URLs for the two above-the-fold weights (400 body, 700 headings),
// so the preloads below never go stale when the dependency updates. Preloading
// 700 prevents the bold hero H1 from reflowing (CLS) when the font swaps in.
import jakarta400 from "@fontsource/plus-jakarta-sans/files/plus-jakarta-sans-latin-400-normal.woff2?url";
import jakarta700 from "@fontsource/plus-jakarta-sans/files/plus-jakarta-sans-latin-700-normal.woff2?url";

import { Header } from "@/components/layout/Header";
import Footer from "@/components/layout/Footer.astro";
import { siteConfig } from "@/config/site";
import {
  generateLocalBusinessSchema,
  generateWebsiteSchema,
} from "@/lib/structured-data";

interface Props {
  title?: string;
  description?: string;
  canonical?: string;
  ogImage?: string;
  ogTitle?: string;
  noindex?: boolean;
  /** Optimized LCP hero image to preload (resolved via getImage on the page). */
  preloadImage?: {
    href: string;
    imagesrcset?: string;
    imagesizes?: string;
    type?: string;
  };
  /** Force the nav into its solid (black) state from the top — for light-hero pages. */
  solidNav?: boolean;
}

const {
  title = `${siteConfig.name} | ${siteConfig.tagline}`,
  description = siteConfig.description,
  canonical,
  ogImage = siteConfig.ogImage,
  ogTitle,
  noindex = false,
  preloadImage,
  solidNav = false,
} = Astro.props as Props;

const canonicalUrl =
  canonical || new URL(Astro.url.pathname, siteConfig.url).toString();
const ogTitleResolved = ogTitle || title;
const absoluteOgImage = new URL(ogImage, siteConfig.url).toString();

const localBusinessSchema = generateLocalBusinessSchema();
const websiteSchema = generateWebsiteSchema();
---

<!doctype html>
<html lang="en" class="h-full">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />

    <title>{title}</title>
    <meta name="description" content={description} />
    <link rel="canonical" href={canonicalUrl} />

    {
      noindex ? (
        <meta name="robots" content="noindex, nofollow" />
      ) : (
        <meta
          name="robots"
          content="index, follow, max-image-preview:large, max-snippet:-1, max-video-preview:-1"
        />
      )
    }

    <meta name="theme-color" content="#a5c021" />
    <meta name="color-scheme" content="light" />
    <meta name="format-detection" content="telephone=yes" />
    <meta name="apple-mobile-web-app-capable" content="yes" />
    <meta name="apple-mobile-web-app-status-bar-style" content="default" />
    <meta name="apple-mobile-web-app-title" content={siteConfig.name} />
    <meta name="application-name" content={siteConfig.name} />

    <link rel="icon" type="image/png" sizes="32x32" href="/favicon-32.png" />
    <link rel="icon" type="image/png" sizes="16x16" href="/favicon-16.png" />
    <link rel="apple-touch-icon" href="/apple-touch-icon.png" />
    <link rel="manifest" href="/manifest.json" />

    {/* Preload the above-the-fold font weights (400 body, 700 headings),
        hash-safe via Vite ?url. crossorigin is required even same-origin or
        the font double-downloads. */}
    <link
      rel="preload"
      as="font"
      type="font/woff2"
      href={jakarta400}
      crossorigin="anonymous"
    />
    <link
      rel="preload"
      as="font"
      type="font/woff2"
      href={jakarta700}
      crossorigin="anonymous"
    />

    {/* Warm up connections to the third-party embed origins (LeadConnector
        quote form). The Lovable visualizer origin is preconnected per-page. */}
    <link rel="preconnect" href="https://link.msgsndr.com" crossorigin />
    <link rel="preconnect" href="https://api.leadconnectorhq.com" crossorigin />

    {/* DNS prefetch for lazy-loaded map embeds */}
    <link rel="dns-prefetch" href="https://www.google.com" />

    {/* Optional LCP image preload (optimized hero from astro:assets) */}
    {
      preloadImage && (
        <link
          rel="preload"
          as="image"
          type={preloadImage.type}
          href={preloadImage.href}
          imagesrcset={preloadImage.imagesrcset}
          imagesizes={preloadImage.imagesizes}
          fetchpriority="high"
        />
      )
    }

    {/* Open Graph */}
    <meta property="og:type" content="website" />
    <meta property="og:site_name" content={siteConfig.name} />
    <meta property="og:title" content={ogTitleResolved} />
    <meta property="og:description" content={description} />
    <meta property="og:url" content={canonicalUrl} />
    <meta property="og:image" content={absoluteOgImage} />
    <meta property="og:image:alt" content={ogTitleResolved} />
    <meta property="og:image:width" content="1200" />
    <meta property="og:image:height" content="630" />
    <meta property="og:locale" content="en_US" />

    {/* Twitter Card */}
    <meta name="twitter:card" content="summary_large_image" />
    <meta name="twitter:title" content={ogTitleResolved} />
    <meta name="twitter:description" content={description} />
    <meta name="twitter:image" content={absoluteOgImage} />
    <meta name="twitter:image:alt" content={ogTitleResolved} />

    <meta name="generator" content={Astro.generator} />

    <script
      type="application/ld+json"
      is:inline
      set:html={JSON.stringify(localBusinessSchema)}
    />
    <script
      type="application/ld+json"
      is:inline
      set:html={JSON.stringify(websiteSchema)}
    />

    <slot name="head" />
  </head>
  <body class="flex min-h-full flex-col">
    <a
      href="#main-content"
      class="sr-only focus:not-sr-only focus:fixed focus:left-4 focus:top-4 focus:z-[10000] focus:rounded-lg focus:bg-primary-500 focus:px-4 focus:py-2 focus:text-sm focus:font-semibold focus:text-black focus:shadow-lg focus:outline-none focus:ring-2 focus:ring-primary-700"
    >
      Skip to main content
    </a>
    <Header client:idle solidNav={solidNav} />
    <main id="main-content" class="flex-1">
      <slot />
    </main>
    <Footer />
  </body>
</html>
