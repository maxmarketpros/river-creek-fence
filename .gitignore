node_modules/
dist/
.astro/

# Raw original working images at the repo root (not used by the build).
# The optimized build pipeline imports from src/assets/stock/, which IS tracked.
/assets/
.env
.env.*
!.env.example
.DS_Store
Thumbs.db
*.log

# Claude Code local user settings (keep launch.json for dev server config)
.claude/settings.local.json
